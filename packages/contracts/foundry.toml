[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.30"
# Pinned below solc's default (prague) so one bytecode runs on both chains; Hedera testnet verified with HederaSmoke (MCOPY executed).
evm_version = "cancun"

[rpc_endpoints]
hedera = "${HEDERA_RPC_URL}"
sepolia = "${SEPOLIA_RPC_URL}"
